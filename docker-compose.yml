services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./backend/.data:/app/backend/.data
    env_file:
      - backend/.env
    environment:
      - PORT=3000
    restart: unless-stopped
