services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - pixel_art_data:/app/backend/.data
    environment:
      - PORT=3000
    restart: unless-stopped

volumes:
  pixel_art_data:
